import { WcaCliConfig } from "../../wca-cli-arguments";
import { CliCommand } from "../cli-command";
/**
 * A CLI command for emitting diagnostics for components.
 */
export declare class DiagnoseCliCommand implements CliCommand {
    id: string;
    /**
     * Prints the help text for this command.
     */
    printHelp(): void;
    /**
     * Runs this command
     * @param config
     * @param inputGlobs
     */
    run(config: WcaCliConfig, ...inputGlobs: string[]): Promise<number>;
}
//# sourceMappingURL=diagnose-cli-command.d.ts.map